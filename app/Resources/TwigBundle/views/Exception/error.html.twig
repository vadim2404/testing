{% extends '::base.html.twig' %}

{% block body %}
<div class="row">
    <div class="span12">
        <div class="hero-unit center">
            <h1>Ошибка <small><font face="Tahoma" color="red">{{ status_code }}</font></small></h1>
            <br />
            <p>{{ status_text }}</p>
            <a href="{{ path('_welcome') }}" class="btn btn-large btn-info"><i class="icon-home icon-white"></i> Домой</a>
        </div>
    </div>
</div>
{% endblock %}
