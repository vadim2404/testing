{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Создание плана</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('plan') }}">
            Вернуться к списку
        </a>
    </li>
</ul>
{% endblock %}
