{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Создать факультет</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('faculty') }}">
            Вернуться к списку
        </a>
    </li>
</ul>
{% endblock %}
